

    # This file contains GK based on ex-4.2

1.
Quc. : How to print lengh of a string ?

1.1 just use python ( len() ) funtion
  Note : just check a string length cannot be perform on normal terminal you may need python shell. another way is you have to print that with print function        using   variable.

2.
Quc. : I want to replace a word from a sentence without edit previous (i.e. admin will be see they have proper direction but after print out will be my words).

2.1 You can use sting slicing again here but with little tricky (i.e. first define a string on a variable {we can call it past variable/string} and then that same     variable {or a new variable if you want to change the variable also} and define new string on this variable {new variable} and use { + } sign to add then the      past variable {which variables need to change} and last use { [] } box brackets with the length the amount of number that you want to replace { you cannot         just use plus number i.e. 2,4,5,6, but you can also use minus number i.e. -2,-4,-6,-9 }
    Note : for more better understand i encorege to read our ( replace_words.py ) file


3.
Quc. : How to slice/extract word from a string

 3.1 just assign a normal string in a variable
 3.2 use that variable (holding the value of string) and warp with ( [] ) curve brakets to slice and inside that curv braket use slicing method to preseve/extract      the word and assign that value to the new variable to saved the value after cut for next
 3.3 just print that to teminal


4.
Quc. : How to concatenate/pair to strings into one

 4.1 first create a string with value and assign to a variable then create another string and assign to another variable
 4.2 now write first variable and add to the second variable using ( + ) (i.e. variable1 + variable2) and assign them to a new variable
 4.3 now the new variable holds the past two variables value
 4.4 now just print out





















